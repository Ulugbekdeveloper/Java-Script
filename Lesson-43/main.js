// let date =  new Date(2023, 1,3)
// date.toUTCString()
// document.cookie = `name=webbrain; expire=${date}`
// document.cookie = `name=webbrain; max-age=1; samesite=lax`
// document.cookie = `title=just; secure; samesite=strict`
// console.log(document.cookie);